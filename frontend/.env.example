# Backend API URL
# This is the base URL for the Django backend API
# During development with Vite proxy, you can use relative paths (/api/v1)
# For production, set this to your deployed backend URL
VITE_API_URL=http://localhost:8000/api/v1
